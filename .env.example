# Vite 开发服务器配置

# 日志级别控制 HMR 更新日志
# 可选值: 'info' | 'warn' | 'error' | 'silent'
# info   - 显示所有日志（默认）
# warn   - 只显示警告和错误
# error  - 只显示错误
# silent - 静默模式，不显示 HMR 日志
VITE_LOG_LEVEL=warn

# 开发服务器端口
VITE_PORT=1420

# 调试模式 - 显示详细的文件监听信息
DEBUG_WATCH_MODE=0

# 强制轮询模式（网络驱动器使用）
FORCE_POLLING=0