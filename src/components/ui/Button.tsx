// src/components/ui/Button.tsx
// module: ui | layer: ui | role: component
// summary: UI 组件

// 文件路径：src/components/ui/Button.tsx
// 兼容层：转发至新版 Button 实现，避免重复定义
export { Button, buttonVariants, type ButtonProps } from './button/Button';