// src/components/contact/XiaohongshuAutoFollow.tsx
// module: ui | layer: ui | role: component
// summary: UI 组件

// DEPRECATED SHIM COMPONENT: Placeholder to satisfy re-exports and legacy pages.
// TODO: Remove after replacing with the unified automation flow.

import React from 'react';

const XiaohongshuAutoFollow: React.FC = () => {
  return (
    <div className="light-theme-force" style={{ background: 'var(--bg-light-base)' }}>
      <p style={{ color: 'var(--text-inverse)' }}>
        XiaohongshuAutoFollow (stub): 此组件已弃用，仅作为类型占位。
      </p>
    </div>
  );
};

export default XiaohongshuAutoFollow;
