# 智能执行系统流程图

## 🎯 执行流程总览

```
┌───────────────────────────────────────────────────────────────┐
│                   智能自动链 / 智能单步                        │
│              execute_chain_test_v3 / execute_single_step      │
└───────────────────┬───────────────────────────────────────────┘
                    │
                    ↓
┌───────────────────────────────────────────────────────────────┐
│              统一执行器 (execute_step_unified)                 │
│          src/exec/v3/unified_step_executor.rs                 │
└───────────────────┬───────────────────────────────────────────┘
                    │
                    ↓
┌───────────────────────────────────────────────────────────────┐
│         核心执行逻辑 (execute_intelligent_analysis_step)       │
│          src/exec/v3/helpers/step_executor.rs                 │
└───────────────────┬───────────────────────────────────────────┘
                    │
                    ↓
        ┌───────────┴──────────┐
        │                      │
        ↓                      ↓
┌──────────────┐      ┌──────────────┐
│ 结构匹配模式  │      │ 传统匹配模式  │
│ (第一优先级)  │      │ (第二优先级)  │
└──────────────┘      └──────────────┘
```

---

## 📊 详细执行流程

### **阶段 0: 配置读取**
```
STEP_STRATEGY_STORE
    │
    ├─ selection_mode
    ├─ batch_config
    └─ structural_signatures ← 关键！
```

### **阶段 1: 结构匹配检测** ⭐ 第一优先级

```rust
┌─────────────────────────────────────────────────────┐
│  检查触发条件                                        │
│  ✓ matchingStrategy == "structural"                 │
│  ✓ structural_signatures 存在                       │
└─────────────────┬───────────────────────────────────┘
                  │
                  ↓
        ┌─────────┴─────────┐
        │ 条件满足？          │
        └─────────┬─────────┘
          YES ←───┘   │
           │          │ NO
           ↓          └──────→ 跳转到阶段2
┌──────────────────────────────────────────┐
│  调用结构匹配引擎                          │
│  v3_match_with_structural_matching        │
│                                           │
│  ┌────────────────────────────────┐      │
│  │ 1. 卡片子树评分                 │      │
│  │    - 骨架匹配                    │      │
│  │    - 层级树评分                  │      │
│  │    - 字段匹配                    │      │
│  └────────────────────────────────┘      │
└──────────────────┬───────────────────────┘
                   │
         ┌─────────┴─────────┐
         │ 匹配成功？          │
         └─────────┬─────────┘
           YES ←───┘   │
            │          │ NO
            ↓          ↓
    ┌────────────┐  ┌──────────────────┐
    │ 返回结果    │  │ explicit_structural? │
    │   ✓        │  └──────────┬───────┘
    └────────────┘      YES ←──┘   │
                         │         │ NO
                         ↓         ↓
                    ┌────────┐  ┌────────┐
                    │ 报错   │  │ 跳转   │
                    │ 退出   │  │ 阶段2  │
                    └────────┘  └────────┘
```

---

### **阶段 2: 传统匹配** ⭐ 第二优先级

```rust
┌───────────────────────────────────────────┐
│  Step 2.1: 收集候选元素                    │
│  collect_candidate_elements               │
│                                           │
│  基于 XPath 匹配                          │
│  ├─ 精确XPath匹配                         │
│  ├─ Resource-id匹配                       │
│  └─ 文本匹配                              │
└───────────────┬───────────────────────────┘
                │
                ↓
┌───────────────────────────────────────────┐
│  Step 2.2: 多候选评估                      │
│  evaluate_best_candidate                  │
│           ↓                                │
│  MultiCandidateEvaluator                  │
│  ::evaluate_candidates                    │
└───────────────┬───────────────────────────┘
                │
                ↓
┌────────────────────────────────────────────────────┐
│        叶子上下文评分系统 (综合评分)                │
│                                                    │
│  ┌──────────────────────────────────────┐         │
│  │ 评分项 1: 子元素文本匹配   +1.0    │ 最高    │
│  │   - 父容器 + 子文本模式              │         │
│  │   - check_child_text_match           │         │
│  └──────────────────────────────────────┘         │
│                                                    │
│  ┌──────────────────────────────────────┐         │
│  │ 评分项 2: Bounds完全匹配   +0.7    │ 次高    │
│  │   - 用户精确选择                     │         │
│  └──────────────────────────────────────┘         │
│                                                    │
│  ┌──────────────────────────────────────┐         │
│  │ 评分项 3: 自身文本匹配     +0.5    │         │
│  │   - 直接文本匹配                     │         │
│  │   - 语义分析 (反义词检测)            │         │
│  └──────────────────────────────────────┘         │
│                                                    │
│  ┌──────────────────────────────────────┐         │
│  │ 评分项 4: Content-desc匹配 +0.3    │         │
│  └──────────────────────────────────────┘         │
│                                                    │
│  ┌──────────────────────────────────────┐         │
│  │ 评分项 5: 可点击性         +0.15   │         │
│  └──────────────────────────────────────┘         │
│                                                    │
│  ┌──────────────────────────────────────┐         │
│  │ 评分项 6: Resource-id匹配  +0.1    │         │
│  └──────────────────────────────────────┘         │
│                                                    │
│  ┌──────────────────────────────────────┐         │
│  │ 评分项 7: 位置偏好         +0.05   │         │
│  └──────────────────────────────────────┘         │
└────────────────┬───────────────────────────────────┘
                 │
                 ↓
       ┌─────────────────┐
       │ 选择最高分候选   │
       │ 返回坐标        │
       └─────────────────┘
```

---

## 🔍 关键决策点

### **决策点 1: 是否启用结构匹配？**
```
条件：
  matchingStrategy == "structural" 
  AND 
  structural_signatures 存在

YES → 结构匹配（卡片子树评分）
NO  → 跳过，直接传统匹配
```

### **决策点 2: 结构匹配失败后？**
```
如果 explicit_structural_mode == true:
    → 报错退出（严格模式）
    
如果 explicit_structural_mode == false:
    → Fallback 到传统匹配（宽松模式）
```

### **决策点 3: 批量模式？**
```
检查 smartSelection.mode:
  - "all"   → 批量全部执行
  - "first" → 单次执行（选最佳候选）
```

---

## 📁 代码位置映射

```
结构匹配引擎:
  src/domain/structure_runtime_match/
    ├─ orchestrator.rs       (协调器)
    ├─ scorers/
    │   ├─ subtree_matcher.rs   (卡片子树评分)
    │   └─ field_scorer.rs      (字段评分)
    └─ skeleton/
        └─ checker_v2.rs        (骨架匹配)

传统匹配引擎:
  src/exec/v3/
    ├─ helpers/step_executor.rs          (主执行逻辑)
    └─ element_matching/
        └─ multi_candidate_evaluator.rs  (叶子上下文评分)

统一入口:
  src/exec/v3/
    ├─ unified_step_executor.rs   (统一执行器)
    ├─ single_step.rs             (智能单步)
    └─ chain_engine.rs            (智能自动链)
```

---

## 🎯 评分权重对比

### **结构匹配评分** (卡片子树)
```
骨架匹配分数:
  - 完整骨架匹配: 高分
  - 部分骨架匹配: 中分
  - 骨架不匹配:   低分/0分

字段匹配分数:
  - 关键字段匹配: 加分
  - 普通字段匹配: 少量加分
  - 字段缺失:     不扣分

层级树评分:
  - 层级完全匹配: 高分
  - 层级部分匹配: 中分
```

### **传统匹配评分** (叶子上下文)
```
总分范围: 0 - 2.0+
最低有效分: 0.3

权重分布:
  子元素文本  1.0  (50%)
  Bounds     0.7  (35%)
  自身文本    0.5  (25%)
  其他       0.6  (30%)
```

---

## 🔄 执行模式

### **智能单步执行**
```
execute_single_step_test_v3
  → execute_step_unified
    → execute_intelligent_analysis_step
      → [阶段1: 结构匹配] OR [阶段2: 传统匹配]
```

### **智能自动链执行**
```
execute_chain_test_v3
  → for each step:
      → execute_step_unified
        → execute_intelligent_analysis_step
          → [阶段1: 结构匹配] OR [阶段2: 传统匹配]
```

两者使用**完全相同**的执行引擎，区别只在：
- 单步：执行一次
- 自动链：循环执行多次（带短路逻辑）

---

## 💡 调整建议

### 如果想强制优先结构匹配：
```rust
// 修改条件判断
let use_structural_matching = 
    has_structural_sigs;  // 移除 explicit_structural_mode 限制
```

### 如果想调整评分权重：
```rust
// 结构匹配权重: src/domain/structure_runtime_match/scorers/
// 传统匹配权重: src/exec/v3/element_matching/multi_candidate_evaluator.rs
```

### 如果想修改fallback行为：
```rust
// 修改 explicit_structural_mode 逻辑
// 当前: false = fallback, true = 报错
// 可改为: 总是fallback
```
